services:
  db:
    image: postgres:17
    restart: always
    environment:
      POSTGRES_USER: devdelivery
      POSTGRES_PASSWORD: devdelivery
      POSTGRES_DB: devdelivery
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: