"use strict";
// Camada Mapper: converte o formato vindo do backend (snake_case/DTO) para o
// formato usado pela UI, se necessário. Hoje o backend já retorna camelCase-friendly,
// então este arquivo fica como ponto de extensão futuro.
