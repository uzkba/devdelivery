// Placeholder — implementar seguindo o mesmo padrão de camadas da feature "cardapio"
// (api -> mappers -> services -> viewmodels -> components/pages), usando POST /auth/login.
export function LoginPage() {
  return <main className="p-4">Login (placeholder)</main>;
}
